script foo(arg1, arg2)
/* This is what the code does */

// error here! - no brackets around computation section 

foreach patient x print y
foreach patient y {
foreach patient z print y
}

table x = y
